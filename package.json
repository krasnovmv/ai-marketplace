{
  "name": "ai-marketplace",
  "private": true,
  "type": "module",
  "engines": {
    "node": ">=22"
  },
  "scripts": {
    "sync": "node --experimental-strip-types src/cli.ts sync",
    "validate": "node --experimental-strip-types src/cli.ts validate",
    "generate": "node --experimental-strip-types src/cli.ts generate",
    "diff": "node --experimental-strip-types src/cli.ts diff",
    "test": "node --experimental-strip-types --test test/*.test.ts",
    "typecheck": "tsc --noEmit"
  },
  "devDependencies": {
    "@types/node": "^26.6.2",
    "typescript": "^7.0.2"
  }
}
