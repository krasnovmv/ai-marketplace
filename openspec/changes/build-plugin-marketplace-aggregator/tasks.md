# Tasks

## 1. Основа и выбор источников

- [x] 1.1 Зафиксировать среду реализации Node.js 22+, ESM JavaScript, npm и Git с учётом AGENTS.md; добавить package.json с sync/validate/generate/diff/test и package-lock.json; проверить `npm ci --ignore-scripts` и `npm test` со встроенным Node test runner.
- [x] 1.2 Реализовать config с marketplace, sources, limits и режимами all/selected; проверить несовместимые поля, пустой selected, exclude, enabled, небезопасные repository/имена и дубликаты до сети.
- [x] 1.3 Подготовить стартовую конфигурацию DietrichGebert/ponytail и mattpocock/skills в all, проверить актуальные ветки и локальные source `./`; убедиться, что OpenSpec не включён в зеркало.

## 2. Чтение и копирование

- [x] 2.1 Реализовать разрешение branch в один SHA и чтение Git tree/blobs без исполнения upstream; проверить на тестовом репозитории сдвиг ветки во время запуска, ошибку доступа и таймаут без утечки секретов.
- [x] 2.2 Реализовать раскрытие all и selected, исключения и прямые каталоги; проверить новый плагин в all, игнорирование соседа в selected, новый skill внутри выбранного пакета, исчезновение, перенос и коллизию.
- [x] 2.3 Реализовать проверки путей, Git modes, структуры, локальных ссылок, лимитов и исполняемых бинарников; проверить traversal, разрешённые внутренние symlink и цепочки, отказ на внешних/сломанных/циклических ссылках и ссылках на каталоги, Windows-имена, коллизию регистра, submodule/LFS и превышение лимита на байт.
- [x] 2.4 Копировать целый выбранный Git-пакет без переписывания и исключений по именам каталогов; проверить сохранность байтов, лицензий, hooks, отслеживаемого dist, исполняемого mode и исходных адресов symlink с mode 120000 (включая Windows-представление), отсутствие .git и неотслеживаемых cache-файлов, отсутствие побочных эффектов postinstall.

## 3. Метаданные и каталоги

- [x] 3.1 Реализовать contentHash и metadata принятой копии; проверить изменение SKILL.md/ресурса/mode, независимость от mtime и сохранение старого metadata при новом SHA с прежними файлами.
- [x] 3.2 Проверить случай двух пакетов одного источника: меняется только один; убедиться, что второй сохраняет свой SHA, а явное изменение происхождения через config отражается отдельно.
- [x] 3.3 Реализовать генерацию Claude и ChatGPT marketplace из принятого/подготовленного набора; сверить поля с официальными форматами, проверить локальные пути от корня, сортировку, стабильный JSON, пустой каталог и отсутствие изменения plugin manifest.
- [x] 3.4 Реализовать локальную validate без upstream-сети и записи; проверить повреждённые файлы/metadata/каталоги и успешную проверку целого текущего набора при ожидающем изменении sources/selection в config.

## 4. Изолированный sync и CLI

- [x] 4.1 Собрать общий конвейер в отдельной копии от Git HEAD с чтением локального желаемого config; проверить, что исходные рабочие файлы, индекс и ветка не изменяются, а незакоммиченные правки остальных файлов не попадают в кандидат.
- [x] 4.2 Ограничить изменение результата управляемыми путями, поддержать явное исключение/отключение и отказ на занятом неизвестном пути; проверить, что сбой любого пакета или записи не даёт публикуемый результат и повтор возможен с чистой копией.
- [x] 4.3 Подключить sync/validate/generate/diff, dry-run и отчёт со статусами и двумя SHA для UNCHANGED; проверить неизвестные аргументы, коды выхода, секреты в DEBUG и побайтовую неизменность проекта после предпросмотра.

## 5. Автоматизация

- [x] 5.1 Создать validate.yml с проверками на Windows/Linux, регенерацией двух каталогов и учётом untracked-файлов; проверить config-only PR для нового источника и падение при повреждённом опубликованном наборе без секретов записи.
- [x] 5.2 Создать sync.yml для ручного запуска, 04:00 UTC и изменения upstreams.json в default branch; проверить concurrency, минимальные права и запрет публикации при ошибке подготовки/валидации.
- [x] 5.3 Реализовать один PR из bot/plugin-sync с отчётом по изменённым пакетам; проверить создание, обновление, no-op при SHA-only, закрытие устаревшего PR после успешного сравнения и сохранение PR при сбое источника.
- [x] 5.4 Проверить актуальность default branch и bot head перед push, отказ на чужих коммитах и публикацию только управляемых файлов; на тестовом запуске убедиться, что выбранный токен запускает необходимые проверки bot PR и слияние остаётся ручным.

## 6. Приёмка зеркала и клиентов

- [ ] 6.1 Проверить сквозной путь: первая поставка → no-op → изменение skill без повышения version → новый плагин в all → игнорируемый сосед в selected → исчезновение upstream с отказом → явное исключение; сверить результаты с семью спецификациями.
- [x] 6.2 Получить реальные стартовые пакеты ponytail и mattpocock-skills из проверенных SHA; проверить полное копирование и лимиты без исправления навыков, зафиксировать отсутствие OpenSpec в обоих каталогах.
- [ ] 6.3 В Claude Desktop подключить личное зеркало и установить стартовые пакеты; проверить обновление после merge, включая прежнюю upstream version, записать режим/версию приложения и штатные действия обновления либо выявленное ограничение.
- [ ] 6.4 Повторить подключение, установку и проверку обновления в поддерживающем локальные плагины режиме ChatGPT desktop; проверить фактически загруженные файлы из кеша без его ручного редактирования.
- [ ] 6.5 Обновить README: назначение зеркала, режимы config, команды, config-only PR, расписание, ручное принятие PR, подключение и обновление обоих клиентов, ограничения и отдельный OpenSpec CLI; выполнить описанные команды на чистой копии.
- [ ] 6.6 После успешной приёмки запустить ручной workflow с реальными источниками и включить расписание; сохранить ссылки на проверенный запуск и PR, подтвердить, что регулярная синхронизация не требует включённого локального компьютера.
